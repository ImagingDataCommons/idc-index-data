This folder contains SQL scripts that are used to generate tables that are
attached to the releases as assets.

To add a new index:

1. Add the sql query in this directory (making sure description of the table and individual columns are included as query comments - see existing queries for examples)
2. Add the name of the new index in 